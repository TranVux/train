import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const TesTabScreen = () => {
  return <View style={{flex: 1}}></View>;
};

export default TesTabScreen;

const styles = StyleSheet.create({});
